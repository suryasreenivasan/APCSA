import java.util.*;
public class Compute_Money {

	public static void main(String[] args) {

		System.out.print("Enter amount of money in dollars.cents: ");
		Scanner input = new Scanner(System.in);
			
		double amount = input.nextDouble();
		input.close();
		
		System.out.println("Your amount of " + amount + "consits of");

		int money = (int)(amount * 100);
	
		int change = (int)amount%100; 
		System.out.println(change + " dollars");
		 change = (int)amount % 25;
		
		
		
		
	
	}

}