/* 2/13/2025
 * String str = "0"; 
 * str += str + 0 + 8;
 * SYstem.out.println(str);
 * 0008
 * 
 * public static boolean containsArt(String s1, String s2, String s3) {
 * 	String all = s1 + s2 + s3;
 * return (all.indexOf("art") != -1);
 * }
 * 
 * double d = Math.Random();
 * double r;
 * range 0.5 <= r < 5.5
 * 2/14/2025
 * 
 * 
 * p 
 * 
 * System.out.println(1 == 2); false
 * if  (true) - if the condition is true, do something. move on
 * if (true) { do something} 
 * else if { do other thing}
 * else { do something else}
 * if the condition is true, do something
 *  if the condition is false but other ocndition is true, do other thing, otherwise do something
 *  else
 *  
 *  p || q
 *  if p is true then p||q is true regardless of q.
 *  q is not evaluated
 *  p && q
 *  if p is false then p && q is false regardless of q
 *  q is not evaluated
 *  
 *  !(q|| q) = !q && !q
 *  !(q && q) = !q || !q
 *  
 *  public static int biggest(int a, int b, int c) {
 *  	if ((a>b) && (a>c)) {
 *  		return a;
 *  	}
 *  	else if ((b > a) && (b > c)) {
 *  		return b;
 *  	}
 *  	else {
 *  		return c;
 *  	}
 *  }
 *  
 *  2/20/20256
 *  public String wordPlay(String word) {
 *  	String str = "";
 *  	for (int k = 0; k < word.length(); k++) {
 *  		if (k % 3 == 0) {
 *  			str = word.substring(k, k+1) + str;
 *  		}
 *  	}
 *  	return str;
 */


public class depths {
	public static void main(String[] args) {
		String string = "hello";
		System.out.println(string.substring(1, 1));
	}
}